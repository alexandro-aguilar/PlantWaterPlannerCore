services:
  localstack:
    container_name: 'localstack_plant-water-planner-core'
    image: localstack/localstack-pro:latest
    ports:
      - '127.0.0.1:4566:4566' # LocalStack Gateway
      - '127.0.0.1:4510-4559:4510-4559' # External services port range
    environment:
      - DEBUG=${DEBUG-}
      - HOST_LAMBDA_DIR=${PWD}
      - DOCKER_HOST=unix:///var/run/docker.sock
      - LOCALSTACK_AUTH_TOKEN=${LOCALSTACK_AUTH_TOKEN}
      - DISABLE_TRANSPARENT_ENDPOINT_INJECTION=1 # to prevent localstack from injecting endpoints into the bedrock client
    networks:
      - localnet
    volumes:
      - '${LOCALSTACK_VOLUME_DIR:-./volume/localstack}:/var/lib/localstack'
      - '/var/run/docker.sock:/var/run/docker.sock'
networks:
  localnet:
    name: localnet
    driver: bridge
